<?xml version="1.0" encoding="UTF-8" ?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence" version="2.1">

    <persistence-unit name="jpatest">
        <properties>
            <property name="javax.persistence.jdbc.driver" value="oracle.jdbc.driver.OracleDriver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:oracle:thin:@oracle.coffit.site:1521:xe"/>
            <property name="javax.persistence.jdbc.user" value="C##CHR"/>
            <property name="javax.persistence.jdbc.password" value="CHR"/>

            <property name="hibernate.show_sql" value="true"/> <!-- 콘솔창에 쿼리문을 보여주는 설정 -->
            <property name="hibernate.format_sql" value="true"/><!-- 쿼리를 이쁘게 만들어준다. -->



           <!--     DB초기화 전략 옵션       -->
            <!--
                 1. none: DDL핸들링 작업을 수행하지 않는다.
                 2. create : 기존에 존재하는 스키마를 삭제하고 새로 생성한다.
                 3. create-drop : 스키마를 생성하고 어플리케이션이 종료될 때 삭제한다.
                 4. update : 기존 스키마를 유지하며 JPA에 의해 변경된 부분만 추가한다.
                 5. validate : 엔티티와 테이블이 정상적으로 매핑되어 있는지만 검증한다.
            -->
        </properties>
    </persistence-unit>
</persistence>